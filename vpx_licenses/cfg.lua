local cfg = {}

cfg.database = true -- True if it take the info from the database, false if you want the licenses to be in inventory

cfg.Boat = "license_boat" -- vrp/cfg/inventory  --  ADD IN inventory cfg if database = false

cfg.Heli = "license_heli"

cfg.Plane = "license_plane"

return cfg